# Sample: TLS-PSK secret for softhsm2-pkcs11-proxy
# Replace with a strong random hex key: openssl rand -hex 16
apiVersion: v1
kind: Secret
metadata:
  name: pkcs11-proxy-psk
  namespace: openbao
type: Opaque
stringData:
  psk: "0123456789abcdef0123456789abcdef"
  identity: "openbao"
